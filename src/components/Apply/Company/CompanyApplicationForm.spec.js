import React from "react";

describe("CompanyApplicationForm", () => {
    describe("interaction", () => {
        it("should fail validation if invalid email", () => {

        });
        it("should fail validation if invalid password", () => {

        });
        it("should fail validation if passwords don't match", () => {

        });
        it("should fail validation if invalid company name", () => {

        });
        it("should fail validation if invalid motivation", () => {

        });
        it("should show submission error after submit", () => {

        });
    });
});
